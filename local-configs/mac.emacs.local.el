;;; init --- my local emacs config for the Mac
;;; Commentary:
;;; Code:

(set-face-attribute 'default nil :height 120)
(set-face-attribute 'default nil :family "Monaco")

(provide 'emacs.local)
;;; .emacs.local.el ends here
